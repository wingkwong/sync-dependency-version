{
  "name": "sync-dependency-version",
  "version": "0.2.2",
  "description": "Synchronise dependency versions between package.json files",
  "keywords": [
    "version",
    "sync",
    "npm",
    "package"
  ],
  "homepage": "https://github.com/wingkwong/sync-dependency-version",
  "repository": {
    "type": "git",
    "url": "https://github.com/wingkwong/sync-dependency-version.git"
  },
  "license": "MIT",
  "author": "WK Wong <wingkwong.code@gmail.com>",
  "packageManager": "pnpm@10.18.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs",
      "require": "./dist/index.js"
    }
  },
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "bin": "./bin/sync-dependency-version.js",
  "files": [
    "bin",
    "dist"
  ],
  "scripts": {
    "build": "pnpm run build:js && pnpm run build:types",
    "build:js": "tsup index.ts --format esm,cjs --clean --minify",
    "build:types": "tsc --emitDeclarationOnly",
    "lint": "oxlint .",
    "lint:fix": "oxlint . --fix",
    "test": "pnpm run build && node --test test/*.test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^15.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "oxlint": "^1.76.0",
    "tsup": "^8.5.1",
    "typescript": "^7.0.2"
  }
}
